// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using NUnit.Framework;
using System;
using System.IO;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class FileSystemTreeTests
    {
        [Test]
        public void TestGetBaseDirectoryPath_ValidPath_ReturnsPath()
        {
            // Arrange
            string expectedPath = @"C:\Users\TestUser\Documents";

            // Act
            using (StringReader sr = new StringReader(expectedPath))
            {
                Console.SetIn(sr);

                string actualPath = GetBaseDirectoryPath();

                // Assert
                Assert.AreEqual(expectedPath, actualPath);
            }
        }

        [Test]
        public void TestGetBaseDirectoryPath_InvalidPath_RetryUntilValidPathEntered()
        {
            // Arrange
            string[] inputPaths = { "invalid", "1234", "C:\\", @"C:\Users\TestUser\Documents" };
            string expectedPath = inputPaths[inputPaths.Length - 1];

            // Act
            using (StringReader sr = new StringReader(string.Join(Environment.NewLine, inputPaths)))
            {
                Console.SetIn(sr);

                string actualPath = GetBaseDirectoryPath();

                // Assert
                Assert.AreEqual(expectedPath, actualPath);
            }
        }

    }
}
