// Test generated by RoostGPT for test efghi using AI Type Open AI and AI Model gpt-4

using System;
using System.Collections.Generic;
using System.Linq;
using NUnit.Framework;
using LunarDoggo.FileSystemTree;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class TestProgram_OutputFileSystemTreeLevel_9fe43fbdc7
    {
        [Test]
        public void TestEmptyFileSystemTreeItem()
        {
            FileSystemTreeItem item = new FileSystemTreeItem
            {
                Name = "Root",
                Type = "Folder",
                Children = new List<FileSystemTreeItem>()
            };

            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);

                Program.OutputFileSystemTreeLevel(0, item);

                string expected = "Root (Folder)\r\n";
                Assert.AreEqual(expected, sw.ToString());
            }
        }

        [Test]
        public void TestFileSystemTreeItemWithChildren()
        {
            FileSystemTreeItem item = new FileSystemTreeItem
            {
                Name = "Root",
                Type = "Folder",
                Children = new List<FileSystemTreeItem>
                {
                    new FileSystemTreeItem
                    {
                        Name = "Child1",
                        Type = "File",
                        Children = new List<FileSystemTreeItem>()
                    },
                    new FileSystemTreeItem
                    {
                        Name = "Child2",
                        Type = "Folder",
                        Children = new List<FileSystemTreeItem>()
                    }
                }
            };

            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);

                Program.OutputFileSystemTreeLevel(0, item);

                string expected = "Root (Folder)\r\n  Child1 (File)\r\n  Child2 (Folder)\r\n";
                Assert.AreEqual(expected, sw.ToString());
            }
        }
    }
}
