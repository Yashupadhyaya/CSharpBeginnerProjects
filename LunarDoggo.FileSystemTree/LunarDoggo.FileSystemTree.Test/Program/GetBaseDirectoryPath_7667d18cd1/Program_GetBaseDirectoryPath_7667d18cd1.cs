// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System;
using System.IO;
using NUnit.Framework;

namespace LunarDoggo.FileSystemTree
{
    [TestFixture]
    public class TestProgram_GetBaseDirectoryPath_7667d18cd1
    {
        [Test]
        public void TestValidDirectoryPath()
        {
            string expectedOutput = "C:\\Windows";
            // TODO: Replace "C:\\Windows" with the actual valid directory path for the test

            using (StringReader sr = new StringReader(expectedOutput))
            {
                Console.SetIn(sr);

                string actualOutput = Program.GetBaseDirectoryPath();

                Assert.AreEqual(expectedOutput, actualOutput);
            }
        }

        [Test]
        public void TestInvalidDirectoryPath()
        {
            string invalidDirectoryPath = "C:\\InvalidDirectory";
            // TODO: Replace "C:\\InvalidDirectory" with an invalid directory path for the test

            using (StringReader sr = new StringReader(invalidDirectoryPath))
            {
                Console.SetIn(sr);

                var ex = Assert.Throws<DirectoryNotFoundException>(() => Program.GetBaseDirectoryPath());
                Assert.That(ex.Message, Is.EqualTo("Could not find a part of the path '" + invalidDirectoryPath + "'."));
            }
        }
    }
}
