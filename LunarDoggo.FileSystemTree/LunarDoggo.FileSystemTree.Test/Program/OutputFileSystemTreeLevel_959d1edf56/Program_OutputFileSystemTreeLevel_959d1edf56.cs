// Test generated by RoostGPT for test int-test using AI Type Open AI and AI Model gpt-3.5-turbo-0301

using NUnit.Framework;
using System.Collections.Immutable;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using System;

namespace LunarDoggo.FileSystemTree
{
    [TestFixture]
    public class FileSystemTreeTest
    {
        [Test]
        public void TestProgram_OutputFileSystemTreeLevel_959d1edf56()
        {
            // Arrange
            var root = new FileSystemTreeItem("root", ItemType.Folder);
            var child1 = new FileSystemTreeItem("child1", ItemType.Folder);
            var child2 = new FileSystemTreeItem("child2", ItemType.File);
            root.Children = ImmutableList.Create(child1, child2);
            var expectedOutput = "root (Folder)\n  child1 (Folder)\n  child2 (File)\n";

            // Act
            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);
                Program.OutputFileSystemTreeLevel(0, root);
                var actualOutput = sw.ToString();

                // Assert
                Assert.AreEqual(expectedOutput, actualOutput);
            }
        }

        [Test]
        public void TestProgram_OutputFileSystemTreeLevel_EmptyTree()
        {
            // Arrange
            var root = new FileSystemTreeItem("root", ItemType.Folder);
            var expectedOutput = "root (Folder)\n";

            // Act
            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);
                Program.OutputFileSystemTreeLevel(0, root);
                var actualOutput = sw.ToString();

                // Assert
                Assert.AreEqual(expectedOutput, actualOutput);
            }
        }
    }
}
