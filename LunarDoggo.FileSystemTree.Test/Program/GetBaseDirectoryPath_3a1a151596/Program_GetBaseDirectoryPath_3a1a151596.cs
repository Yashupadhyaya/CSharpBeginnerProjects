// Test generated by RoostGPT for test test-csharp5 using AI Type Open AI and AI Model gpt-4

using System;
using System.IO;
using NUnit.Framework;
using LunarDoggo.FileSystemTree; // Include the namespace where the Program class is located

namespace LunarDoggo.FileSystemTree.Test
{
    [TestFixture]
    public class Program_GetBaseDirectoryPath_3a1a151596
    {
        [Test]
        public void GetBaseDirectoryPath_ValidInput_ReturnsCorrectPath()
        {
            var expectedPath = Path.GetTempPath();
            Console.SetIn(new StringReader(expectedPath));

            var actualPath = Program.GetBaseDirectoryPath();

            Assert.AreEqual(expectedPath, actualPath);
        }

        [Test]
        public void GetBaseDirectoryPath_InvalidInput_PromptsUntilValidPath()
        {
            var invalidPath = "InvalidPath";
            var validPath = Path.GetTempPath();
            var reader = new StringReader(invalidPath + Environment.NewLine + validPath);
            Console.SetIn(reader);

            var actualPath = Program.GetBaseDirectoryPath();

            Assert.AreEqual(validPath, actualPath);
        }
    }
}
