// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System;
using System.IO;
using NUnit.Framework;
using LunarDoggo.FileSystemTree;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class ProgramTests
    {
        [Test]
        public void TestProgram_Main_e1a933a43f()
        {
            using (var stringWriter = new StringWriter())
            {
                Console.SetOut(stringWriter);

                Program.Main(new string[0]);

                var output = stringWriter.ToString();
                Assert.IsTrue(output.Contains("FileSystemTreeItem"));
            }
        }

        [Test]
        public void TestProgram_Main_InvalidDirectory()
        {
            using (var stringWriter = new StringWriter())
            {
                Console.SetOut(stringWriter);

                // TODO: Change the value of Program.baseDirectoryPath to an invalid directory path
                Program.baseDirectoryPath = "invalid/directory/path";

                Assert.Throws<DirectoryNotFoundException>(() => Program.Main(new string[0]));
            }
        }
    }
}
