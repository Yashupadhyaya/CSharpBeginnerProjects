// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using NUnit.Framework;
using System;
using System.IO;

namespace YourTestProjectNamespace
{
    [TestFixture]
    public class ConsoleVisualizer_DrawNoQuestions_Test
    {
        private StringWriter stringWriter;
        private TextWriter originalOutput;

        [SetUp]
        public void Setup()
        {
            // Redirect Console.WriteLine() to StringWriter
            stringWriter = new StringWriter();
            originalOutput = Console.Out;
            Console.SetOut(stringWriter);
        }

        [TearDown]
        public void Cleanup()
        {
            // Restore Console.WriteLine() back to original output
            Console.SetOut(originalOutput);
            stringWriter.Dispose();
        }

        [Test]
        public void DrawNoQuestions_ShouldPrintErrorMessage_WhenNoQuestionsLoaded()
        {
            // Arrange
            ConsoleVisualizer consoleVisualizer = new ConsoleVisualizer();

            // Act
            consoleVisualizer.DrawNoQuestions();

            // Assert
            Assert.AreEqual("No questions were loaded, please enter some questions into the json file in the applications folder.\n\nReload game? (Y/N)\n", stringWriter.ToString());
        }

        [Test]
        public void DrawNoQuestions_ShouldNotPrintErrorMessage_WhenQuestionsLoaded()
        {
            // Arrange
            ConsoleVisualizer consoleVisualizer = new ConsoleVisualizer();

            // Act
            consoleVisualizer.DrawNoQuestions();

            // Assert
            Assert.AreNotEqual("No questions were loaded, please enter some questions into the json file in the applications folder.\n\nReload game? (Y/N)\n", stringWriter.ToString());
        }
    }
}
