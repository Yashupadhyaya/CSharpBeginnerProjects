// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System.Collections.Generic;
using System.Linq;
using System.IO;
using System;
using NUnit.Framework;

namespace LunarDoggo.FileSystemTree
{
    public class FileSystemTreeItem
    {
        public string Name { get; set; }
        public string Type { get; set; }
        public List<FileSystemTreeItem> Children { get; set; }

        public FileSystemTreeItem(string name, string type, List<FileSystemTreeItem> children)
        {
            Name = name;
            Type = type;
            Children = children;
        }
    }

    public class Program
    {
        public static void OutputFileSystemTreeLevel(int level, FileSystemTreeItem item)
        {
            Console.WriteLine(new string(' ', level * 2) + item.Name + " (" + item.Type + ")");
            foreach (var child in item.Children)
            {
                OutputFileSystemTreeLevel(level + 1, child);
            }
        }
    }

    [TestFixture]
    public class TestProgram_OutputFileSystemTreeLevel_9fe43fbdc7
    {
        [Test]
        public void TestWithValidFileSystemTreeItem()
        {
            FileSystemTreeItem item = new FileSystemTreeItem("test", "file", new List<FileSystemTreeItem>());
            using(StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);
                Program.OutputFileSystemTreeLevel(0, item);

                string expected = "test (file)\n";
                Assert.AreEqual(expected, sw.ToString());
            }
        }

        [Test]
        public void TestWithFileSystemTreeItemHavingChildren()
        {
            FileSystemTreeItem childItem = new FileSystemTreeItem("child", "file", new List<FileSystemTreeItem>());
            FileSystemTreeItem item = new FileSystemTreeItem("parent", "directory", new List<FileSystemTreeItem>(){ childItem });
            
            using(StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);
                Program.OutputFileSystemTreeLevel(0, item);

                string expected = "parent (directory)\n  child (file)\n";
                Assert.AreEqual(expected, sw.ToString());
            }
        }
    }
}
