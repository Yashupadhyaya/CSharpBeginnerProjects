// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using NUnit.Framework;
using LunarDoggo.FileSystemTree;
using System.Collections.Generic;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class FileSystemTreeItemTests
    {

        // Test case for initializing a file system tree item with no children
        [Test]
        public void TestInitializingWithNoChildren()
        {
            // Arrange
            string name = "File";
            FileSystemTreeItemType type = FileSystemTreeItemType.File;
            IEnumerable<FileSystemTreeItem> children = new List<FileSystemTreeItem>();

            // Act
            FileSystemTreeItem item = new FileSystemTreeItem(name, type, children);

            // Assert
            Assert.AreEqual(item.Name, name);
            Assert.AreEqual(item.Type, type);
            Assert.AreEqual(item.ChildrenCount, 0);
        }

        // Test case for initializing a file system tree item with children
        [Test]
        public void TestInitializingWithChildren()
        {
            // Arrange
            string name = "Folder";
            FileSystemTreeItemType type = FileSystemTreeItemType.Folder;
            IEnumerable<FileSystemTreeItem> children = new List<FileSystemTreeItem>()
            {
                new FileSystemTreeItem("File1", FileSystemTreeItemType.File, null),
                new FileSystemTreeItem("File2", FileSystemTreeItemType.File, null)
            };

            // Act
            FileSystemTreeItem item = new FileSystemTreeItem(name, type, children);

            // Assert
            Assert.AreEqual(item.Name, name);
            Assert.AreEqual(item.Type, type);
            Assert.AreEqual(item.ChildrenCount, 2);
        }
    }
}
