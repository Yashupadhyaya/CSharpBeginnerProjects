// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using System.Collections.Generic;
using LunarDoggo.FileSystemTree;
using NUnit.Framework;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class FileSystemTreeItemTests
    {
        // Test case for a file item
        [Test]
        public void TestFileSystemTreeItem_FileItem()
        {
            // Arrange
            string name = "example.txt";
            FileSystemTreeItemType type = FileSystemTreeItemType.File;
            IEnumerable<FileSystemTreeItem> children = null;

            // Act
            FileSystemTreeItem fileItem = new FileSystemTreeItem(name, type, children);

            // Assert
            Assert.AreEqual(name, fileItem.Name);
            Assert.AreEqual(type, fileItem.Type);
            Assert.IsNull(fileItem.Children);
        }

        // Test case for a directory item with children
        [Test]
        public void TestFileSystemTreeItem_DirectoryItemWithChildren()
        {
            // Arrange
            string name = "folder";
            FileSystemTreeItemType type = FileSystemTreeItemType.Directory;
            IEnumerable<FileSystemTreeItem> children = new List<FileSystemTreeItem>
            {
                new FileSystemTreeItem("file1.txt", FileSystemTreeItemType.File, null),
                new FileSystemTreeItem("file2.txt", FileSystemTreeItemType.File, null),
                new FileSystemTreeItem("subfolder", FileSystemTreeItemType.Directory,
                    new List<FileSystemTreeItem>
                    {
                        new FileSystemTreeItem("file3.txt", FileSystemTreeItemType.File, null)
                    })
            };

            // Act
            FileSystemTreeItem directoryItem = new FileSystemTreeItem(name, type, children);

            // Assert
            Assert.AreEqual(name, directoryItem.Name);
            Assert.AreEqual(type, directoryItem.Type);
            Assert.AreEqual(children, directoryItem.Children);
        }
    }
}
