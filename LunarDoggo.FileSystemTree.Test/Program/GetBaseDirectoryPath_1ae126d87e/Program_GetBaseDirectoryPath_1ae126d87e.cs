// Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

using System;
using System.IO;
using NUnit.Framework;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class Program_GetBaseDirectoryPath_1ae126d87e
    {
        [Test]
        public void GetBaseDirectoryPath_ValidPath_ReturnsPath()
        {
            var program = new Program();
            using (var sw = new StringWriter())
            {
                Console.SetOut(sw);
                using (var sr = new StringReader(@"C:\\Windows"))
                {
                    Console.SetIn(sr);
                    var result = program.GetBaseDirectoryPath();
                    Assert.AreEqual(@"C:\\Windows", result);
                }
            }
        }

        [Test]
        public void GetBaseDirectoryPath_InvalidPath_PromptsUntilValidPath()
        {
            var program = new Program();
            using (var sw = new StringWriter())
            {
                Console.SetOut(sw);
                using (var sr = new StringReader("InvalidPath\nC:\\Windows"))
                {
                    Console.SetIn(sr);
                    var result = program.GetBaseDirectoryPath();
                    Assert.AreEqual(@"C:\\Windows", result);
                    StringAssert.Contains("Please enter a valid directory path:", sw.ToString());
                }
            }
        }
    }
}
