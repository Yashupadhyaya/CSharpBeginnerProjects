// ********RoostGPT********
// Test generated by RoostGPT for test csharp-testing using AI Type Open AI and AI Model gpt-4



// ********RoostGPT********
using System;
using System.IO;
using NUnit.Framework;
using LunarDoggo.QuizGame.Visuals;

namespace LunarDoggo.QuizGame.Visuals.Test
{
    [TestFixture]
    public class DrawGameResult_127a94cb29
    {
        private ConsoleVisualizer visualizer;
        private StringWriter stringWriter;

        [SetUp]
        public void SetUp()
        {
            visualizer = new ConsoleVisualizer();
            stringWriter = new StringWriter();
            Console.SetOut(stringWriter);
        }
        
        [TearDown]
        public void TearDown()
        {
            stringWriter.Close();
            stringWriter.Dispose();
            Console.ResetColor();
        }

        [Test]
        public void DrawGameResult_AllQuestionsCorrect_DisplaySuccessMessage()
        {
            int totalQuestions = 5;
            int correctAnswers = 5;

            visualizer.DrawGameResult(totalQuestions, correctAnswers);

            string expectedOutput = string.Format("You got {0} out of {1} question right. Continue with \"enter\".\n\n", correctAnswers, totalQuestions);
            Assert.AreEqual(expectedOutput, stringWriter.ToString());
        }

        [Test]
        public void DrawGameResult_NoQuestionCorrect_DisplayFailureMessage()
        {
            int totalQuestions = 5;
            int correctAnswers = 0;

            visualizer.DrawGameResult(totalQuestions, correctAnswers);

            string expectedOutput = string.Format("You got {0} out of {1} question right. Continue with \"enter\".\n\n", correctAnswers, totalQuestions);
            Assert.AreEqual(expectedOutput, stringWriter.ToString());
        }
    }
}
