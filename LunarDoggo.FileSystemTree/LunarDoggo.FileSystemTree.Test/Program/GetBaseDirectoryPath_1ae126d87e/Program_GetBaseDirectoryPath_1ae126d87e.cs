// Test generated by RoostGPT for test int-test using AI Type Open AI and AI Model gpt-3.5-turbo-0301

using System.Collections.Immutable;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using System;

namespace TestProject
{
    [TestClass]
    public class TestProgram
    {
        [TestMethod]
        public void TestProgram_GetBaseDirectoryPath_1ae126d87e()
        {
            // Test Case 1: Valid directory path
            string expected1 = "C:\\Users\\Test\\Documents";
            string actual1 = GetBaseDirectoryPath();
            Assert.AreEqual(expected1, actual1);

            // Test Case 2: Invalid directory path
            string expected2 = "Please enter a valid directory path: ";
            string actual2;

            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);
                Console.SetIn(new StringReader("invalid_path"));
                GetBaseDirectoryPath();
                actual2 = sw.ToString().Trim();
            }

            Assert.AreEqual(expected2, actual2);
        }

        private static string GetBaseDirectoryPath()
        {
            string path;
            do
            {
                Console.Clear();
                Console.Write("Please enter a valid directory path: ");
                path = Console.ReadLine();
            } while (!Directory.Exists(path));
            return path;
        }
    }
}
