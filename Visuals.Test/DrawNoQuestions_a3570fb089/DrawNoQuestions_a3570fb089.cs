// ********RoostGPT********
// Test generated by RoostGPT for test csharp-testing using AI Type Open AI and AI Model gpt-4



// ********RoostGPT********
using System;
using NUnit.Framework;
using LunarDoggo.QuizGame.Visuals;
using System.IO;

namespace LunarDoggo.QuizGame.Visuals.Test
{
    [TestFixture]
    public class DrawNoQuestions_a3570fb089
    {
        private ConsoleVisualizer consoleVisualizer;
        private StringWriter stringWriter;
        private StringReader stringReader;

        [SetUp]
        public void Setup()
        {
            consoleVisualizer = new ConsoleVisualizer();
            stringWriter = new StringWriter();
            Console.SetOut(stringWriter);
        }

        [TearDown]
        public void TearDown()
        {
            stringWriter.Dispose();
            if (stringReader != null)
            {
                stringReader.Dispose();
            }
        }

        [Test]
        public void DrawNoQuestions_MessageAppearsInConsoleOutput()
        {
            consoleVisualizer.DrawNoQuestions();
            string consoleOutput = stringWriter.ToString();
            Assert.IsTrue(consoleOutput.Contains("No questions were loaded, please enter some questions into the json file in the applications folder.\n\nReload game? (Y/N)"));
        }

        [Test]
        public void DrawNoQuestions_ConsoleOutputDoesNotContainOtherMessages()
        {
            consoleVisualizer.DrawNoQuestions();
            string consoleOutput = stringWriter.ToString();
            Assert.IsFalse(consoleOutput.Contains("You got"));
            Assert.IsFalse(consoleOutput.Contains("question right. Continue with \"enter\"."));
            Assert.IsFalse(consoleOutput.Contains("Do you like to play again? (Y/N)"));
        }
    }
}
