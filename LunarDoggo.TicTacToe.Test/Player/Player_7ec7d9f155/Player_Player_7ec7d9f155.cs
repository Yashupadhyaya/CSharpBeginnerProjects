// Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

using NUnit.Framework;

namespace LunarDoggo.TicTacToe.Test
{
    public class Player
    {
        public byte Id { get; set; }
        public string Display { get; set; }

        public Player(byte id, string display)
        {
            Id = id;
            Display = display;
        }
    }

    [TestFixture]
    public class PlayerTests
    {
        [Test]
        public void TestPlayer_Player_7ec7d9f155()
        {
            // Arrange
            byte id = 1;
            string display = "Player1";

            // Act
            Player player = new Player(id, display);

            // Assert
            Assert.AreEqual(id, player.Id);
            Assert.AreEqual(display, player.Display);
        }

        [Test]
        public void TestPlayer_Player_7ec7d9f155_WithEmptyDisplay()
        {
            // Arrange
            byte id = 1;
            string display = string.Empty;

            // Act
            Player player = new Player(id, display);

            // Assert
            Assert.AreEqual(id, player.Id);
            Assert.AreEqual(display, player.Display);
        }

        [Test]
        public void TestPlayer_Player_7ec7d9f155_WithNullDisplay()
        {
            // Arrange
            byte id = 1;
            string display = null;

            // Act
            Player player = new Player(id, display);

            // Assert
            Assert.AreEqual(id, player.Id);
            Assert.AreEqual(display, player.Display);
        }

        [Test]
        public void TestPlayer_Player_7ec7d9f155_WithMaxByteValue()
        {
            // Arrange
            byte id = byte.MaxValue;
            string display = "Player1";

            // Act
            Player player = new Player(id, display);

            // Assert
            Assert.AreEqual(id, player.Id);
            Assert.AreEqual(display, player.Display);
        }
    }
}
