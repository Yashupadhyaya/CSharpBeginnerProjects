// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using NUnit.Framework;
using System;
using System.Collections.Generic;
using LunarDoggo.FileSystemTree;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class FileSystemTreeItemTests
    {
        [Test]
        public void TestFileSystemTreeItem_WithValidInputs()
        {
            // Arrange
            string name = "Folder";
            FileSystemTreeItemType type = FileSystemTreeItemType.Folder;
            List<FileSystemTreeItem> children = new List<FileSystemTreeItem>();

            // Act
            FileSystemTreeItem item = new FileSystemTreeItem(name, type, children);

            // Assert
            Assert.AreEqual(item.Name, name);
            Assert.AreEqual(item.Type, type);
            Assert.AreEqual(item.Children, children);
        }

        [Test]
        public void TestFileSystemTreeItem_WithNullName()
        {
            // Arrange
            string name = null;
            FileSystemTreeItemType type = FileSystemTreeItemType.File;
            List<FileSystemTreeItem> children = new List<FileSystemTreeItem>();

            // Assert
            Assert.Throws<ArgumentNullException>(() =>
            {
                // Act
                FileSystemTreeItem item = new FileSystemTreeItem(name, type, children);
            });
        }

        [Test]
        public void TestFileSystemTreeItem_WithNullChildren()
        {
            // Arrange
            string name = "File";
            FileSystemTreeItemType type = FileSystemTreeItemType.File;
            List<FileSystemTreeItem> children = null;

            // Act
            FileSystemTreeItem item = new FileSystemTreeItem(name, type, children);

            // Assert
            Assert.AreEqual(item.Name, name);
            Assert.AreEqual(item.Type, type);
            Assert.IsNull(item.Children);
        }
    }
}
