// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System;
using NUnit.Framework;
using LunarDoggo.TicTacToe;

namespace LunarDoggo.TicTacToe.Test
{
    [TestFixture]
    public class TestGameBoard
    {
        [Test]
        public void TestGameBoard_HasWon_eaaba27540()
        {
            // Arrange
            var gameBoard = new GameBoard();
            byte player = 1; // TODO: Change the player value as per your test scenario

            // Act
            var result = gameBoard.HasWon(player);

            // Assert
            Assert.IsTrue(result, "Expected the player to have won the game, but the HasWon method returned false.");
        }

        [Test]
        public void TestGameBoard_HasNotWon_eaaba27540()
        {
            // Arrange
            var gameBoard = new GameBoard();
            byte player = 2; // TODO: Change the player value as per your test scenario

            // Act
            var result = gameBoard.HasWon(player);

            // Assert
            Assert.IsFalse(result, "Expected the player to have not won the game, but the HasWon method returned true.");
        }
    }
}
