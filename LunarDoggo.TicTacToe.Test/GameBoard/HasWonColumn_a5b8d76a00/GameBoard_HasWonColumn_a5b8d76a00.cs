// Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

using System;
using NUnit.Framework;

namespace TicTacToe
{
    public class GameBoard
    {
        public byte[,] tiles = new byte[3, 3];

        public bool HasWonColumn(byte player, int columnIndex)
        {
            return tiles[0, columnIndex] == player && tiles[1, columnIndex] == player && tiles[2, columnIndex] == player;
        }
    }

    [TestFixture]
    public class TestSuite
    {
        private GameBoard _gameBoard;

        [SetUp]
        public void Setup()
        {
            _gameBoard = new GameBoard();
        }

        [Test]
        public void TestGameBoard_HasWonColumn_a5b8d76a00()
        {
            // Arrange
            byte player = 1;
            int columnIndex = 1;

            // Act
            _gameBoard.tiles[0, columnIndex] = player;
            _gameBoard.tiles[1, columnIndex] = player;
            _gameBoard.tiles[2, columnIndex] = player;

            // Assert
            Assert.IsTrue(_gameBoard.HasWonColumn(player, columnIndex));
        }

        [Test]
        public void TestGameBoard_HasNotWonColumn_a5b8d76a00()
        {
            // Arrange
            byte player = 1;
            int columnIndex = 1;

            // Act
            _gameBoard.tiles[0, columnIndex] = player;
            _gameBoard.tiles[1, columnIndex] = 2; // Different player
            _gameBoard.tiles[2, columnIndex] = player;

            // Assert
            Assert.IsFalse(_gameBoard.HasWonColumn(player, columnIndex));
        }
    }
}
