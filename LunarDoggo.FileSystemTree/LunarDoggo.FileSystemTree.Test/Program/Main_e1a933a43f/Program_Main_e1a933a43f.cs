// Test generated by RoostGPT for test efghi using AI Type Open AI and AI Model gpt-4

using System;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using System.Collections.Immutable;
using NUnit.Framework;

namespace LunarDoggo.FileSystemTree
{
    [TestFixture]
    public class ProgramTests
    {
        [Test]
        public void TestProgram_Main_e1a933a43f()
        {
            // Arrange
            var args = new string[] { };
            var expectedBaseDirectoryPath = "expected path"; // TODO: Replace with the expected base directory path
            var expectedFileSystemTree = new FileSystemTreeItem(); // TODO: Replace with the expected FileSystemTreeItem object

            // Act
            using (var sw = new StringWriter())
            {
                Console.SetOut(sw);

                Program.Main(args);

                // Assert
                Assert.AreEqual(expectedBaseDirectoryPath, Program.GetBaseDirectoryPath());
                Assert.AreEqual(expectedFileSystemTree, Program.GetFileSystemTree(new DirectoryInfo(expectedBaseDirectoryPath)));
                Assert.AreEqual(expectedFileSystemTree.ToString(), sw.ToString().Trim());
            }
        }

        [Test]
        public void TestProgram_Main_InvalidDirectory()
        {
            // Arrange
            var args = new string[] { "invalid directory" };

            // Act and Assert
            var ex = Assert.Throws<DirectoryNotFoundException>(() => Program.Main(args));
            Assert.That(ex.Message, Is.EqualTo("The system cannot find the path specified."));
        }
    }
}
