// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System;
using System.Collections.Immutable;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using NUnit.Framework;

namespace LunarDoggo.FileSystemTree.Tests
{
    public enum FileSystemTreeItemType
    {
        File,
        Directory
    }

    public class FileSystemTreeItem
    {
        public string Name { get; set; }
        public FileSystemTreeItemType Type { get; set; }
        public List<FileSystemTreeItem> Children { get; set; }
    }

    public class Program
    {
        public static FileSystemTreeItem GetFileSystemTree(DirectoryInfo baseDirectory)
        {
            // Method implementation here
            return null;
        }
    }

    [TestFixture]
    public class FileSystemTreeTests
    {
        [Test]
        public void TestGetFileSystemTreeWithValidDirectory()
        {
            DirectoryInfo baseDirectory = new DirectoryInfo("validDirectoryPath"); // TODO: replace with a valid directory path
            FileSystemTreeItem result = Program.GetFileSystemTree(baseDirectory);

            Assert.IsNotNull(result);
            Assert.AreEqual(baseDirectory.Name, result.Name);
            Assert.AreEqual(FileSystemTreeItemType.Directory, result.Type);
            Assert.IsTrue(result.Children.Any());
        }

        [Test]
        public void TestGetFileSystemTreeWithEmptyDirectory()
        {
            DirectoryInfo baseDirectory = new DirectoryInfo("emptyDirectoryPath"); // TODO: replace with an empty directory path
            FileSystemTreeItem result = Program.GetFileSystemTree(baseDirectory);

            Assert.IsNotNull(result);
            Assert.AreEqual(baseDirectory.Name, result.Name);
            Assert.AreEqual(FileSystemTreeItemType.Directory, result.Type);
            Assert.IsFalse(result.Children.Any());
        }

        [Test]
        public void TestGetFileSystemTreeWithInvalidDirectory()
        {
            DirectoryInfo baseDirectory = new DirectoryInfo("invalidDirectoryPath"); // TODO: replace with an invalid directory path

            var ex = Assert.Throws<DirectoryNotFoundException>(() => Program.GetFileSystemTree(baseDirectory));
            Assert.That(ex.Message, Is.EqualTo("Could not find a part of the path 'invalidDirectoryPath'."));
        }
    }
}
