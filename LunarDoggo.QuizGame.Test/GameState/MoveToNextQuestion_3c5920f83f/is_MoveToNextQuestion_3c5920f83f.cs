// Test generated by RoostGPT for test test-csrepo using AI Type Open AI and AI Model gpt-4

using NUnit.Framework;
using Moq;
using LunarDoggo.QuizGame;
using System.Collections.Generic;

namespace LunarDoggo.QuizGame.Test
{
    [TestFixture]
    public class is_MoveToNextQuestion_3c5920f83f
    {
        private Mock<GameState> gameStateMock;
        private GameState gameState;

        [SetUp]
        public void Setup()
        {
            gameStateMock = new Mock<GameState>();
            gameState = gameStateMock.Object;
        }

        [Test]
        public void MoveToNextQuestion_UnansweredQuestionsExist_CurrentQuestionIsSet()
        {
            // Arrange
            gameStateMock.SetupProperty(g => g.HasUnansweredQuestions, true);
            gameStateMock.SetupProperty(g => g.UnansweredQuestions, new List<Question> { new Question(), new Question() });

            // Act
            gameState.MoveToNextQuestion();

            // Assert
            Assert.IsNotNull(gameState.CurrentQuestion);
            Assert.IsFalse(gameState.IsCurrentQuestionAnswered);
            Assert.AreEqual(0, gameState.HighlightedAnswerIndex);
            Assert.IsNull(gameState.ChosenAnswer);
        }

        [Test]
        public void MoveToNextQuestion_NoUnansweredQuestions_CurrentQuestionIsNull()
        {
            // Arrange
            gameStateMock.SetupProperty(g => g.HasUnansweredQuestions, false);

            // Act
            gameState.MoveToNextQuestion();

            // Assert
            Assert.IsNull(gameState.CurrentQuestion);
        }

        [TearDown]
        public void Teardown()
        {
            gameStateMock = null;
            gameState = null;
        }
    }
}
