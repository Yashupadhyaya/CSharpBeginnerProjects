// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System;
using System.IO;
using NUnit.Framework;
using LunarDoggo.FileSystemTree;

namespace TestNamespace
{
    [TestFixture]
    public class TestClass
    {
        [Test]
        public void TestValidDirectoryPath()
        {
            // TODO: Replace with a valid directory path in your system
            string validDirectoryPath = "C:\\Users\\Username\\Documents";

            using (StringReader sr = new StringReader(validDirectoryPath))
            {
                Console.SetIn(sr);

                string resultPath = GetBaseDirectoryPath();

                Assert.AreEqual(validDirectoryPath, resultPath);
            }
        }

        [Test]
        public void TestInvalidDirectoryPath()
        {
            // TODO: Replace with an invalid directory path
            string invalidDirectoryPath = "C:\\Invalid\\Path";

            using (StringReader sr = new StringReader(invalidDirectoryPath))
            {
                Console.SetIn(sr);

                Assert.Throws<DirectoryNotFoundException>(() => GetBaseDirectoryPath());
            }
        }

        private static string GetBaseDirectoryPath()
        {
            string path;
            do
            {
                Console.Clear(); //Clear the console window
                Console.Write("Please enter a valid directory path: ");
                path = Console.ReadLine();

                //While the user input is not a valid path and therefore doesn't exist, continue to prompt for a valid directory path
            } while (!Directory.Exists(path));
            return path;
        }
    }
}
