// Test generated by RoostGPT for test efghi using AI Type Open AI and AI Model gpt-4

using System;
using System.Collections.Generic;
using System.Collections.Immutable;
using System.IO;
using System.Linq;
using NUnit.Framework;
using LunarDoggo.FileSystemTree;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class ProgramTests
    {
        [Test]
        public void TestProgram_OutputFileSystemTreeLevel_9fe43fbdc7()
        {
            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);

                var children = new List<FileSystemTreeItem>
                {
                    new FileSystemTreeItem { Name = "Child1", Type = "Folder", Children = ImmutableList<FileSystemTreeItem>.Empty },
                    new FileSystemTreeItem { Name = "Child2", Type = "File", Children = ImmutableList<FileSystemTreeItem>.Empty }
                };

                var item = new FileSystemTreeItem { Name = "Root", Type = "Folder", Children = children.ToImmutableList() };

                Program.OutputFileSystemTreeLevel(0, item);

                string expected = string.Format("Root (Folder){0}  Child1 (Folder){0}  Child2 (File){0}", Environment.NewLine);

                Assert.AreEqual(expected, sw.ToString());
            }
        }

        [Test]
        public void TestProgram_OutputFileSystemTreeLevel_NullChildren_9fe43fbdc7()
        {
            using (StringWriter sw = new StringWriter())
            {
                Console.SetOut(sw);

                var item = new FileSystemTreeItem { Name = "Root", Type = "Folder", Children = null };

                Program.OutputFileSystemTreeLevel(0, item);

                string expected = string.Format("Root (Folder){0}", Environment.NewLine);

                Assert.AreEqual(expected, sw.ToString());
            }
        }
    }
}
