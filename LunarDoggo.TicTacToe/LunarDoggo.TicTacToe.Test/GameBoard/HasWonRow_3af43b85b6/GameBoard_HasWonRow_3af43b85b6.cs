// Test generated by RoostGPT for test test-dummy using AI Type Open AI and AI Model gpt-4

using System;
using NUnit.Framework;
using LunarDoggo.TicTacToe; // Add this line to reference the GameBoard class

namespace TicTacToe
{
    [TestFixture]
    public class TestGameBoard
    {
        private GameBoard gameBoard;

        [SetUp]
        public void Setup()
        {
            gameBoard = new GameBoard();
        }

        [Test]
        public void TestGameBoard_HasWonRow_3af43b85b6()
        {
            byte player = 1;
            int rowIndex = 0;
            
            gameBoard.tiles[rowIndex, 0] = 1;
            gameBoard.tiles[rowIndex, 1] = 1;
            gameBoard.tiles[rowIndex, 2] = 1;
            
            bool result = gameBoard.HasWonRow(player, rowIndex);
            Assert.IsTrue(result, "Expected player 1 to have won the row");
        }

        [Test]
        public void TestGameBoard_HasNotWonRow_3af43b85b6()
        {
            byte player = 1;
            int rowIndex = 0;
            
            gameBoard.tiles[rowIndex, 0] = 1;
            gameBoard.tiles[rowIndex, 1] = 0;
            gameBoard.tiles[rowIndex, 2] = 1;
            
            bool result = gameBoard.HasWonRow(player, rowIndex);
            Assert.IsFalse(result, "Expected player 1 to have not won the row");
        }
    }
}
