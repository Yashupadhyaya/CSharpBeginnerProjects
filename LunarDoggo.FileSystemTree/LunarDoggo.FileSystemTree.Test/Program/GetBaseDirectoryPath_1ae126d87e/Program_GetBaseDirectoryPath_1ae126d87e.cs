// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using LunarDoggo.FileSystemTree.Tests;
using NUnit.Framework;
using System;
using System.IO;

namespace LunarDoggo.FileSystemTree.Tests
{
    [TestFixture]
    public class FileSystemTests
    {
        [Test]
        public void TestGetBaseDirectoryPath_ValidDirectory()
        {
            // Arrange
            string expectedPath = @"C:\";
            string inputDirectory = @"C:\";

            // Act
            using (StringReader sr = new StringReader(inputDirectory))
            {
                Console.SetIn(sr);

                string actualPath = Program.GetBaseDirectoryPath();

                // Assert
                Assert.AreEqual(expectedPath, actualPath);
            }
        }

        [Test]
        public void TestGetBaseDirectoryPath_InvalidDirectoryThenValidDirectory()
        {
            // Arrange
            string expectedPath = @"C:\";
            string inputDirectory = "InvalidDir" + Environment.NewLine + @"C:\";

            // Act
            using (StringReader sr = new StringReader(inputDirectory))
            {
                Console.SetIn(sr);

                string actualPath = Program.GetBaseDirectoryPath();

                // Assert
                Assert.AreEqual(expectedPath, actualPath);
            }
        }
    }
}
