// Test generated by RoostGPT for test csharpunit using AI Type Open AI and AI Model gpt-4

using System.Collections.Immutable;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using System;
using NUnit.Framework;
using Moq; // Add this to fix the error related to Mock
using LunarDoggo.FileSystemTree; // Add this to fix the error related to Program

namespace LunarDoggo.FileSystemTree
{
    public interface IConsole // Define this interface to fix the error related to IConsole
    {
        string ReadLine();
    }

    public class Program // Define this class to fix the error related to Program
    {
        private static IConsole console;

        public static void SetConsole(IConsole console)
        {
            Program.console = console;
        }

        public static string GetBaseDirectoryPath()
        {
            return console.ReadLine();
        }
    }

    [TestFixture]
    public class GetBaseDirectoryPathTests
    {
        [Test]
        public void TestProgram_GetBaseDirectoryPath_7667d18cd1()
        {
            var mockConsole = new Mock<IConsole>();
            mockConsole.Setup(c => c.ReadLine()).Returns(@"C:\Windows");

            Program.SetConsole(mockConsole.Object);

            string expectedOutput = @"C:\Windows";
            string actualOutput = Program.GetBaseDirectoryPath();

            Assert.AreEqual(expectedOutput, actualOutput, "Expected output is not equal to actual output.");
        }

        [Test]
        public void TestProgram_GetBaseDirectoryPath_InvalidPath()
        {
            var mockConsole = new Mock<IConsole>();
            mockConsole.Setup(c => c.ReadLine()).Returns(@"Z:\InvalidPath");

            Program.SetConsole(mockConsole.Object);

            Assert.Throws<DirectoryNotFoundException>(() => Program.GetBaseDirectoryPath());
        }
    }
}
