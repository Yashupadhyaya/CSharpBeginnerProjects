// Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

using NUnit.Framework;

// Assuming the TicTacToe and Game classes are in the namespace 'TicTacToeGame'
using TicTacToeGame;

namespace TicTacToeTests
{
    [TestFixture]
    public class GameTests
    {
        private TicTacToeGame game;

        [SetUp]
        public void Setup()
        {
            game = new TicTacToeGame();
        }

        [Test]
        public void TestGameState_SwitchPlayer_4613b1d601()
        {
            // Arrange
            var firstPlayer = game.currentPlayer;

            // Act
            game.SwitchPlayer();
            var afterFirstSwitch = game.currentPlayer;

            // Assert
            Assert.AreNotEqual(firstPlayer, afterFirstSwitch, "After first switch, current player should be different.");

            // Act
            game.SwitchPlayer();
            var afterSecondSwitch = game.currentPlayer;

            // Assert
            Assert.AreEqual(firstPlayer, afterSecondSwitch, "After second switch, current player should be the same as initial player.");
        }
    }
}
