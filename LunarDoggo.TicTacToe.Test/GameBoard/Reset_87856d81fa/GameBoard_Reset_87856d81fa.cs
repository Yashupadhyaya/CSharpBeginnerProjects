// Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

using System;
using NUnit.Framework;

namespace TicTacToeTests
{
    public class TicTacToe
    {
        public class GameBoard
        {
            public int[,] tiles = new int[3, 3];

            public void Reset()
            {
                for (int x = 0; x < tiles.GetLength(0); x++)
                {
                    for (int y = 0; y < tiles.GetLength(1); y++)
                    {
                        tiles[x, y] = 0;
                    }
                }
            }
        }
    }

    [TestFixture]
    public class GameBoardTests
    {
        private TicTacToe.GameBoard gameBoard;

        [SetUp]
        public void Setup()
        {
            gameBoard = new TicTacToe.GameBoard();
            // TODO: Set the initial state of the game board here if necessary
        }

        [TearDown]
        public void Teardown()
        {
            gameBoard = null;
        }

        [Test]
        public void TestGameBoard_Reset_87856d81fa()
        {
            // Arrange
            // TODO: Set the game board to a non-reset state

            // Act
            gameBoard.Reset();

            // Assert
            for (int x = 0; x < gameBoard.tiles.GetLength(0); x++)
            {
                for (int y = 0; y < gameBoard.tiles.GetLength(1); y++)
                {
                    Assert.AreEqual(0, gameBoard.tiles[x, y]);
                }
            }
        }

        [Test]
        public void TestGameBoard_Reset_WithAlreadyResetBoard()
        {
            // Arrange
            gameBoard.Reset();

            // Act
            gameBoard.Reset();

            // Assert
            for (int x = 0; x < gameBoard.tiles.GetLength(0); x++)
            {
                for (int y = 0; y < gameBoard.tiles.GetLength(1); y++)
                {
                    Assert.AreEqual(0, gameBoard.tiles[x, y]);
                }
            }
        }
    }
}
