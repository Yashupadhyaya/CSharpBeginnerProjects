// Test generated by RoostGPT for test unt-test using AI Type Azure Open AI and AI Model roost-gpt

using NUnit.Framework;
using System.IO;

[TestFixture]
public class FileQuizQuestionSerializer_GetFileContent_601537cfbe
{
    private string _validFilePath;
    private string _invalidFilePath;

    [SetUp]
    public void Setup()
    {
        // TODO: Set the path to a valid file
        _validFilePath = "C:\\path\\to\\valid\\file.txt";

        // TODO: Set the path to an invalid file
        _invalidFilePath = "C:\\path\\to\\invalid\\file.txt";
    }

    [Test]
    public void GetFileContent_ValidFilePath_ReturnsFileContent()
    {
        // Arrange
        var expectedContent = "This is the content of the file.";

        // Act
        var result = GetFileContent(_validFilePath);

        // Assert
        Assert.AreEqual(expectedContent, result);
    }

    [Test]
    public void GetFileContent_InvalidFilePath_ReturnsEmptyString()
    {
        // Act
        var result = GetFileContent(_invalidFilePath);

        // Assert
        Assert.AreEqual(string.Empty, result);
    }

    [Test]
    public void GetFileContent_FileDoesNotExist_ReturnsEmptyString()
    {
        // Arrange
        var nonExistentFilePath = "C:\\path\\to\\nonexistent\\file.txt";

        // Act
        var result = GetFileContent(nonExistentFilePath);

        // Assert
        Assert.AreEqual(string.Empty, result);
    }

    private string GetFileContent(string filePath)
    {
        if (File.Exists(filePath))
        {
            return File.ReadAllText(filePath);
        }
        return string.Empty;
    }
}
