// Test generated by RoostGPT for test int-test using AI Type Open AI and AI Model gpt-3.5-turbo-0301

using System;
using System.IO;
using NUnit.Framework;

namespace TestNamespace
{
    [TestFixture]
    public class TestClass
    {
        [Test]
        public void TestProgram_GetBaseDirectoryPath_1ae126d87e()
        {
            // Test case 1: Valid directory path is entered
            // Expected output: The entered directory path should be returned
            string expectedOutput = Directory.GetCurrentDirectory(); // TODO: Change this to a valid directory path on your machine
            Console.SetIn(new StringReader(expectedOutput));
            string actualOutput = GetBaseDirectoryPath();
            Assert.AreEqual(expectedOutput, actualOutput);

            // Test case 2: Invalid directory path is entered
            // Expected output: The user should be prompted to enter a valid directory path again
            string invalidDirectoryPath = "invalid/path";
            string[] inputs = { invalidDirectoryPath, expectedOutput };
            int inputIndex = 0;
            Console.SetIn(new StringReader(invalidDirectoryPath + Environment.NewLine + expectedOutput));
            actualOutput = GetBaseDirectoryPath();
            Assert.AreEqual(expectedOutput, actualOutput);
        }

        private static string GetBaseDirectoryPath()
        {
            string path;
            do
            {
                Console.Clear(); //Clear the console window
                Console.Write("Please enter a valid directory path: ");
                path = Console.ReadLine();

                //While the user input is not a valid path and therefor doesn't exist, continue to prompt for a valid directory path
            } while (!Directory.Exists(path));
            return path;
        }
    }
}
