// Test generated by RoostGPT for test demo5 using AI Type Open AI and AI Model gpt-4

using NUnit.Framework;
using LunarDoggo.QuizGame;
using System.Collections.Generic;
using System.Linq;

namespace LunarDoggo.QuizGame.Test
{
    [TestFixture]
    public class is_GameState_411dc2bb34
    {
        private GameState _gameState;

        [SetUp]
        public void Setup()
        {
            _gameState = null;
        }

        [Test]
        public void GameState_WithNonNullQuestions_InitializesCorrectly()
        {
            // Arrange
            var questions = new List<QuizQuestion>
            {
                new QuizQuestion(),
                new QuizQuestion(),
                new QuizQuestion()
            };

            // Act
            _gameState = new GameState(questions);

            // Assert
            Assert.IsNotNull(_gameState);
            Assert.AreEqual(questions.Count, _gameState.TotalQuestionCount);
        }

        [Test]
        public void GameState_WithNullQuestions_InitializesCorrectly()
        {
            // Arrange
            IEnumerable<QuizQuestion> questions = null;

            // Act
            _gameState = new GameState(questions);

            // Assert
            Assert.IsNotNull(_gameState);
            Assert.AreEqual(0, _gameState.TotalQuestionCount);
        }

        [Test]
        public void GameState_WithEmptyQuestions_InitializesCorrectly()
        {
            // Arrange
            var questions = new List<QuizQuestion>();

            // Act
            _gameState = new GameState(questions);

            // Assert
            Assert.IsNotNull(_gameState);
            Assert.AreEqual(0, _gameState.TotalQuestionCount);
        }
    }
}
